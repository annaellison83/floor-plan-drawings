function portalPage() {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="robots" content="noindex,nofollow">
  <title>FloorPlanDrawings · Master</title>
  <style>
    :root{--ink:#25332f;--muted:#58675f;--paper:#fbfaf6;--surface:#fff;--sage:#e5eee5;--sage-strong:#a8c5ae;--forest:#194b3e;--blue:#205bb5;--attention:#f5ebd5;--attention-ink:#765619;--line:#e4e8e0;--input:#87998d;--danger:#a94444;--shadow:0 2px 7px rgba(38,61,54,.04)}
    *{box-sizing:border-box}html{background:var(--paper)}body{margin:0;background:linear-gradient(145deg,#f5f6f0 0%,var(--paper) 48%,#eef4ee 100%);color:var(--ink);font:14px/1.45 Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,select,textarea{font:inherit}button{cursor:pointer}.portal{width:100%;padding:24px clamp(16px,2.3vw,32px) 64px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:20px;padding-bottom:18px;border-bottom:1px solid rgba(228,232,224,.78)}.brand{display:flex;align-items:center;gap:12px}.mark{width:88px;height:46px;border-radius:14px;background:var(--forest);display:flex;flex-direction:column;align-items:center;justify-content:center;color:#fff;font-size:14px;font-weight:800;letter-spacing:.1em;line-height:1}.mark small{font-size:8px;letter-spacing:.16em;margin-top:5px;opacity:.8}.eyebrow{font-size:11px;letter-spacing:.16em;text-transform:uppercase;color:var(--muted);font-weight:700}.brand h1{font-size:19px;line-height:1.1;margin:3px 0 0;letter-spacing:-.03em}.pill{display:inline-flex;align-items:center;border:1px solid rgba(196,208,198,.58);background:rgba(255,255,255,.72);border-radius:999px;padding:6px 11px;font-size:12px;color:var(--muted)}.heading{display:flex;align-items:end;justify-content:space-between;gap:20px;margin:22px 0 16px}.heading h2{font-size:28px;line-height:1.1;letter-spacing:-.045em;margin:5px 0}.heading p{margin:0;color:var(--muted);max-width:640px}.heading-actions{display:flex;gap:8px;flex-wrap:wrap}.button{border:0;border-radius:8px;padding:10px 14px;background:var(--forest);color:#fff;font-weight:700;min-height:40px;box-shadow:0 2px 5px rgba(25,75,62,.12)}.button.secondary{background:var(--surface);color:var(--forest);border:1px solid var(--line);box-shadow:none}.button:focus-visible,.chip:focus-visible,.select:focus-visible,.search:focus-visible,.close:focus-visible,.field input:focus-visible,.field select:focus-visible,.field textarea:focus-visible{outline:3px solid #3b82f6;outline-offset:2px}.button:disabled{opacity:.55;cursor:not-allowed}.controls{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;margin:20px 0 12px}.chips{display:flex;gap:6px;flex-wrap:wrap}.chip{border:1px solid var(--line);background:rgba(255,255,255,.72);color:var(--muted);border-radius:8px;padding:8px 11px;min-height:38px;font-size:13px}.chip.active{background:var(--forest);border-color:var(--forest);color:#fff}.control-right{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.select,.search{border:1px solid var(--line);background:var(--surface);border-radius:8px;padding:9px 11px;min-height:38px;color:var(--ink)}.select{min-width:160px}.search{min-width:min(280px,100%);outline:none}.search:focus{border-color:var(--sage-strong);box-shadow:0 0 0 3px rgba(168,197,174,.25)}.clear{border:0;background:transparent;color:var(--blue);text-decoration:underline;padding:8px;font-size:13px}.summary{display:flex;align-items:center;gap:8px;color:var(--muted);font-size:13px;margin:8px 0 12px}.summary strong{color:var(--ink)}.save-confirm{margin:8px 0 12px;padding:10px 12px;border-radius:8px;background:var(--sage);color:var(--forest);font-weight:700}.stale{margin:8px 0 12px;padding:10px 12px;border-radius:8px;background:var(--attention);color:var(--attention-ink)}.table-shell{width:100%;overflow-x:auto;border-radius:12px}.jobs-table{width:100%;min-width:1370px;border-collapse:separate;border-spacing:0 8px}.jobs-table thead th{padding:0 12px 4px;text-align:left;color:var(--muted);font-size:11px;letter-spacing:.08em;text-transform:uppercase;font-weight:700;white-space:nowrap}.jobs-table tbody tr{background:rgba(255,255,255,.92);box-shadow:var(--shadow);cursor:default}.jobs-table tbody td{padding:12px;border-top:1px solid rgba(228,232,224,.74);border-bottom:1px solid rgba(228,232,224,.74);vertical-align:top;color:var(--ink)}.jobs-table tbody td:first-child{border-left:1px solid rgba(228,232,224,.74);border-radius:12px 0 0 12px}.jobs-table tbody td:last-child{border-right:1px solid rgba(228,232,224,.74);border-radius:0 12px 12px 0}.jobs-table .client{font-weight:600;min-width:145px}.jobs-table .address{min-width:220px;max-width:330px;white-space:normal}.jobs-table .date{white-space:nowrap;color:var(--muted)}.jobs-table .status-cell{min-width:125px}.status{display:inline-flex;align-items:center;white-space:normal;max-width:100%;line-height:1.2;border-radius:6px;padding:5px 8px;background:var(--sage);color:var(--forest);font-size:11px;font-weight:750}.status.warning{background:var(--attention);color:var(--attention-ink)}.status.muted{background:#f0f2ef;color:#69756e}.link-group{display:flex;gap:5px;flex-wrap:wrap;min-width:188px}.link{display:inline-flex;align-items:center;border:1px solid rgba(183,204,224,.62);border-radius:7px;background:#f8fbff;color:var(--blue);text-decoration:underline;padding:5px 7px;font-size:12px;font-weight:650;white-space:nowrap}.unavailable{display:inline-block;min-width:34px;min-height:1em;color:transparent;font-size:12px}.area{font-variant-numeric:tabular-nums;white-space:nowrap}.area small{display:block;margin-top:2px;color:var(--muted);font-size:11px}.confirmed-pill{display:inline-flex;align-items:center;margin-left:6px;padding:3px 6px;border-radius:999px;background:var(--sage);color:var(--forest);font-size:10px;font-weight:750;vertical-align:middle}.jobs-cards{display:none}.mobile-card{background:rgba(255,255,255,.92);border:1px solid rgba(228,232,224,.78);border-radius:12px;padding:16px;box-shadow:var(--shadow);margin-bottom:10px;cursor:default}.mobile-card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.mobile-card h3{font-size:20px;line-height:1.2;letter-spacing:-.03em;margin:2px 0 4px}.mobile-address{font-size:16px;color:var(--muted);line-height:1.45}.mobile-meta{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin:13px 0}.mobile-meta div{padding:9px 10px;background:#f5f8f4;border-radius:8px}.mobile-meta span{display:block;color:var(--muted);font-size:10px;letter-spacing:.08em;text-transform:uppercase;font-weight:700}.mobile-meta strong{display:block;margin-top:2px;font-size:13px}.mobile-area{border-radius:8px;background:#f5f8f4;padding:11px;margin:10px 0}.mobile-area strong{font-variant-numeric:tabular-nums}.mobile-area small{display:block;color:var(--muted);margin-top:3px}.mobile-links{display:flex;gap:6px;flex-wrap:wrap;margin:11px 0}.mobile-card details{border-top:1px solid rgba(228,232,224,.8);padding-top:10px;margin-top:10px;color:var(--muted)}.mobile-card summary{cursor:pointer;color:var(--forest);font-weight:700}.empty{padding:42px 22px;text-align:center;background:rgba(255,255,255,.74);border:1px dashed rgba(190,205,194,.8);border-radius:12px;color:var(--muted)}.empty strong{color:var(--ink)}.skeleton{height:46px;border-radius:8px;background:linear-gradient(90deg,#f0f3ef,#fafbf9,#f0f3ef);background-size:200% 100%;animation:load 1.4s ease-in-out infinite}@keyframes load{from{background-position:200% 0}to{background-position:-200% 0}}.backdrop{position:fixed;inset:0;background:rgba(24,39,34,.3);display:none;align-items:center;justify-content:center;padding:16px;z-index:5}.backdrop.open{display:flex}.panel{width:min(680px,100%);max-height:calc(100vh - 32px);overflow:auto;background:var(--paper);border:1px solid rgba(228,232,224,.9);border-radius:14px;padding:22px;box-shadow:0 4px 16px rgba(38,61,54,.12)}.panel-head{display:flex;justify-content:space-between;gap:15px;align-items:flex-start}.panel h2{font-size:25px;line-height:1.1;margin:5px 0}.close{border:0;background:transparent;color:var(--muted);font-size:24px;line-height:1;padding:4px}.form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:20px}.field{display:flex;flex-direction:column;gap:6px}.field.full{grid-column:1/-1}.field label{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);font-weight:700}.field input,.field select,.field textarea{width:100%;border:1px solid var(--input);border-radius:8px;background:var(--surface);padding:10px 11px;color:var(--ink);outline:none;font-size:16px}.field textarea{min-height:84px;resize:vertical}.panel-foot{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}.notice{margin-top:12px;padding:10px 12px;border-radius:8px;background:var(--attention);color:var(--attention-ink);font-size:13px}.login{position:fixed;inset:0;background:linear-gradient(145deg,#edf4ee,#fbfaf6);display:flex;align-items:center;justify-content:center;padding:20px;z-index:10}.login.hidden{display:none}.login-card{width:min(430px,100%);background:var(--surface);border:1px solid var(--line);border-radius:14px;padding:26px;box-shadow:0 4px 16px rgba(38,61,54,.08)}.login-card h2{margin:7px 0;font-size:28px;letter-spacing:-.045em}.login-card p{color:var(--muted);margin:0 0 18px}.login-card input{width:100%;border:1px solid var(--input);border-radius:8px;padding:11px;font-size:16px}.login-card .button{width:100%;margin-top:10px}.error{color:var(--danger);font-size:13px;margin-top:10px;min-height:20px}.toast{position:fixed;right:16px;bottom:16px;background:var(--forest);color:#fff;padding:11px 14px;border-radius:8px;box-shadow:0 4px 16px rgba(38,61,54,.12);display:none;z-index:20}.toast.show{display:block}
    @media(max-width:767px){body{font-size:16px}.portal{padding:16px 16px 48px}.topbar{padding-bottom:14px}.pill{display:none}.heading{align-items:flex-start;flex-direction:column;margin:18px 0 14px}.heading h2{font-size:25px}.heading-actions{width:100%}.heading-actions .button{flex:1}.controls{align-items:stretch;flex-direction:column;margin-top:16px}.control-right{align-items:stretch;flex-direction:column}.search,.select{width:100%;min-width:0;min-height:44px}.chips{gap:6px}.chip{min-height:44px;padding:10px 12px}.summary{margin-top:5px}.table-shell{overflow:visible}.jobs-table{display:none}.jobs-cards{display:block}.mobile-card{padding:16px}.form{grid-template-columns:1fr}.field.full{grid-column:auto}.panel{padding:18px;border-radius:14px}.panel-foot{position:sticky;bottom:-18px;background:var(--paper);padding:12px 0 2px}.panel-foot .button{flex:1;min-height:44px}.toast{left:16px;right:16px;bottom:16px;text-align:center}}
    @media(prefers-reduced-motion:reduce){*,*:before,*:after{animation-duration:.01ms!important;animation-iteration-count:1!important;scroll-behavior:auto!important}}
    .intake-dropbox{margin:0 0 18px;padding:18px 20px;background:rgba(255,255,255,.78);border:1px solid var(--line);border-radius:16px;box-shadow:var(--shadow)}.intake-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:12px}.intake-head h3{margin:3px 0 4px;font-size:17px;letter-spacing:-.02em}.intake-head p{margin:0;color:var(--muted);max-width:720px}.intake-form{display:grid;grid-template-columns:minmax(0,1fr) 250px auto;gap:10px;align-items:end}.intake-form textarea,.intake-form input{width:100%;border:1px solid var(--line);background:#fff;border-radius:10px;padding:11px 12px;color:var(--ink);outline:none}.intake-form textarea{min-height:76px;resize:vertical}.intake-form textarea:focus,.intake-form input:focus{border-color:var(--sage-strong);box-shadow:0 0 0 3px rgba(168,197,174,.22)}.intake-field label{display:block;font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--muted);font-weight:700;margin:0 0 5px}.intake-result{margin-top:10px;padding:10px 12px;border-radius:9px;background:var(--sage);color:var(--forest);font-size:13px}.intake-result.error{background:#fae8e5;color:var(--danger)}
  </style>
  <style>
    .property-links>div{display:flex;flex-direction:row;align-items:flex-start;flex-wrap:nowrap;gap:5px}.jobs-table{table-layout:fixed;min-width:1360px}.jobs-table th:nth-child(1),.jobs-table td:nth-child(1){width:200px}.jobs-table th:nth-child(2),.jobs-table td:nth-child(2){width:130px}.jobs-table th:nth-child(3),.jobs-table td:nth-child(3){width:90px}.jobs-table th:nth-child(4),.jobs-table td:nth-child(4){width:180px}.jobs-table th:nth-child(5),.jobs-table td:nth-child(5){width:120px}.jobs-table th:nth-child(6),.jobs-table td:nth-child(6){width:95px}.jobs-table th:nth-child(7),.jobs-table td:nth-child(7){width:160px}.jobs-table th:nth-child(8),.jobs-table td:nth-child(8){width:120px}.jobs-table th:nth-child(9),.jobs-table td:nth-child(9){width:95px}.jobs-table th:nth-child(10),.jobs-table td:nth-child(10){width:170px}.jobs-table td{overflow-wrap:anywhere}.jobs-table .address{min-width:0}.jobs-table .property-links{min-width:0}.jobs-table .area{min-width:0;white-space:normal}.jobs-table td:nth-child(5){white-space:normal}.property-links .link{white-space:nowrap;padding-left:5px;padding-right:5px}.intake-dropzone{min-height:76px;border:1px dashed #9bb2a2;border-radius:10px;background:#f7faf5;display:flex;align-items:center;justify-content:center;text-align:center;padding:12px;color:var(--muted);font-size:13px;transition:.15s ease}.intake-dropzone:hover,.intake-dropzone:focus-visible,.intake-dropzone.dragging{border-color:var(--forest);background:var(--sage);color:var(--forest);outline:none}.intake-dropzone strong{display:block;color:var(--forest)}.intake-file-note{margin-top:6px;font-size:12px;color:var(--muted)}
    .scope-toggle{display:inline-flex;gap:4px;padding:3px;border:1px solid var(--line);border-radius:9px;background:rgba(255,255,255,.72)}.scope-button{border:0;background:transparent;color:var(--muted);border-radius:6px;padding:6px 9px;font-size:12px;font-weight:700}.scope-button.active{background:var(--forest);color:#fff}
    .header-actions{display:flex;align-items:center;justify-content:flex-end;gap:8px;flex-wrap:wrap}.nav-active{text-decoration:none;background:var(--sage)!important;border-color:var(--sage-strong)!important}.control-right{display:grid;grid-template-columns:auto minmax(160px,auto) minmax(0,1fr);align-items:center;gap:8px;flex:1 1 520px;width:min(100%,780px);margin-left:auto}.control-right .clear{grid-column:1;grid-row:1}.control-right .select{grid-column:2;grid-row:1;width:100%}.control-right .search{grid-column:3;grid-row:1;width:100%;min-width:0}
    .table-shell{overflow:visible}.table-shell.table-scroll{overflow:auto;max-height:calc(100dvh - var(--controls-sticky-offset,58px) - 16px)}.table-shell.table-scroll thead th{top:0}
    @media(max-width:767px){.topbar{align-items:flex-start;flex-direction:column}.header-actions{width:100%;display:grid;grid-template-columns:1fr 1fr}.header-actions .button:last-child{grid-column:1/-1}.intake-form{grid-template-columns:1fr}.intake-form .button{width:100%;min-height:44px}.intake-head{display:block}.control-right{display:flex;width:100%;flex-basis:auto}}
    .snapshot{margin:0 0 18px;padding:16px 18px;background:rgba(229,238,229,.72);border:1px solid rgba(168,197,174,.62);border-radius:16px}.snapshot-head{display:flex;align-items:baseline;justify-content:space-between;gap:12px;margin-bottom:12px}.snapshot-head h3{margin:3px 0 0;font-size:18px;letter-spacing:-.02em}.snapshot-date{color:var(--muted);font-size:12px}.snapshot-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.snapshot-card{background:rgba(255,255,255,.78);border:1px solid rgba(168,197,174,.58);border-radius:11px;padding:11px 12px}.snapshot-card strong{display:block;font-size:24px;line-height:1.1;color:var(--forest);font-variant-numeric:tabular-nums}.snapshot-card span{display:block;margin-top:3px;color:var(--muted);font-size:11px;letter-spacing:.04em;text-transform:uppercase;font-weight:700}.snapshot-list{display:flex;flex-wrap:wrap;gap:6px;margin-top:11px}.snapshot-item{display:inline-flex;align-items:center;gap:5px;padding:6px 8px;border-radius:8px;background:rgba(255,255,255,.7);color:var(--ink);font-size:12px}.snapshot-item b{color:var(--forest)}.snapshot-item.attention{background:var(--attention);color:var(--attention-ink)}.intake-dropbox[hidden]{display:none}
    [hidden]{display:none!important}.control-left{display:flex;align-items:center;gap:12px;flex-wrap:wrap;min-width:0;flex:1 1 auto}.control-left .summary{margin:0;white-space:nowrap}.controls{position:sticky;top:0;z-index:4;background:var(--paper);padding:10px 0 8px;margin:0 0 2px;box-shadow:0 8px 14px -14px rgba(38,61,54,.55)}.control-right{min-width:0}.clear.button{padding:9px 12px;text-decoration:none;white-space:nowrap}.jobs-table thead th{position:sticky;top:var(--controls-sticky-offset,58px);z-index:3;background:var(--paper);box-shadow:0 2px 7px -7px rgba(38,61,54,.65)}
    .topbar{justify-content:flex-end}.portal{height:100dvh;display:flex;flex-direction:column;overflow:hidden;padding-bottom:20px}.topbar{flex:0 0 auto}.controls{position:static;flex:0 0 auto;flex-wrap:nowrap;gap:14px;margin:10px 0 8px;padding:0;background:transparent;box-shadow:none}.control-left{flex:0 0 auto;flex-wrap:nowrap}.control-left .summary{margin:0;white-space:nowrap}.control-right{flex:1 1 auto;width:auto;min-width:0}.control-right .search{min-width:0}.table-shell{flex:1 1 auto;min-height:0;overflow:auto!important;max-height:none!important}.table-shell.table-scroll{max-height:none!important}.jobs-table thead th{position:sticky;top:0!important;z-index:3;background:var(--paper);box-shadow:0 2px 7px -7px rgba(38,61,54,.65)}.status{width:max-content;max-width:100%;white-space:nowrap}.jobs-table .status-cell{min-width:115px}
    @media(max-width:767px){.snapshot-head{display:block}.snapshot-date{display:block;margin-top:3px}.snapshot-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.snapshot-card strong{font-size:22px}}
    @media(max-width:767px){.portal{height:auto;min-height:100dvh;overflow:visible;padding-bottom:48px}.topbar{justify-content:flex-start}.controls{flex-wrap:wrap;align-items:stretch;margin-top:10px}.control-left{flex-wrap:wrap}.control-right{width:100%;flex:1 1 100%}.table-shell{flex:initial;overflow:visible!important;max-height:none!important}.table-shell.table-scroll{max-height:none!important}}
    .topbar{justify-content:space-between}.control-right{grid-template-columns:minmax(160px,auto) minmax(0,1fr)}.control-right .select{grid-column:1}.control-right .search{grid-column:2}.jobs-table thead{background:var(--paper)}.jobs-table thead th{top:8px!important}.column-sort{display:inline-flex;align-items:center;gap:5px;width:100%;border:0;background:transparent;color:var(--muted);padding:0;text-align:left;font-size:inherit;letter-spacing:inherit;text-transform:inherit;font-weight:inherit;cursor:pointer}.column-sort:hover{color:var(--forest)}.column-sort:focus-visible{outline:3px solid #3b82f6;outline-offset:2px;border-radius:3px}.column-sort-indicator{min-width:10px;color:var(--forest);font-size:12px;font-weight:800}.sort-active{color:var(--forest)}
    .status{width:fit-content;max-width:88px;white-space:normal;line-height:1.15}.jobs-table thead{position:sticky;top:0;z-index:4;background:var(--paper)}.jobs-table thead th{box-shadow:0 -8px 0 var(--paper),0 8px 0 var(--paper),0 2px 7px -7px rgba(38,61,54,.65)}.jobs-table .address .address-street,.jobs-table .address .address-locality,.mobile-address .address-street,.mobile-address .address-locality{display:block}.jobs-table .address .address-locality,.mobile-address .address-locality{margin-top:2px;color:var(--muted);font-size:12px;line-height:1.25}.status-cell{vertical-align:top}.panel-head-actions{display:flex;align-items:flex-start;gap:8px}.all-info-panel{width:min(1100px,100%)}.all-info-scroll{overflow:auto;max-height:calc(100vh - 170px);border:1px solid var(--line);border-radius:10px;background:var(--surface)}.all-info-table{width:100%;min-width:820px;border-collapse:collapse}.all-info-table th,.all-info-table td{padding:9px 11px;border-bottom:1px solid var(--line);text-align:left;vertical-align:top}.all-info-table th{width:220px;background:#f1f5ef;color:var(--muted);font-size:11px;letter-spacing:.06em;text-transform:uppercase;white-space:nowrap}.all-info-table td{white-space:pre-wrap;word-break:break-word}.all-info-table tr:last-child th,.all-info-table tr:last-child td{border-bottom:0}.workflow-check{display:inline-flex;align-items:center;justify-content:center;min-width:48px;padding:5px 6px;border-radius:7px;background:#eef2ee;color:#849188;font-size:11px;font-weight:800}.workflow-check.done{background:var(--sage);color:var(--forest)}.jobs-table.essential{min-width:1250px}.jobs-table.essential th:nth-child(1),.jobs-table.essential td:nth-child(1){width:205px}.jobs-table.essential th:nth-child(2),.jobs-table.essential td:nth-child(2){width:135px}.jobs-table.essential th:nth-child(3),.jobs-table.essential td:nth-child(3){width:82px}.jobs-table.essential th:nth-child(4),.jobs-table.essential td:nth-child(4){width:165px}.jobs-table.essential th:nth-child(n+5),.jobs-table.essential td:nth-child(n+5){width:80px}.jobs-table.essential th:nth-child(n+5) .column-sort{min-width:0;white-space:normal;overflow-wrap:anywhere;line-height:1.15}.jobs-table.complete{min-width:1360px}
    .topbar{gap:14px}.topbar>.controls{position:static;display:flex;align-items:center;flex:1 1 auto;min-width:0;flex-wrap:nowrap;gap:10px;margin:0;padding:0;background:transparent;box-shadow:none}.topbar>.controls .control-left{flex:0 0 auto;flex-wrap:nowrap}.topbar>.controls .control-right{display:flex;align-items:center;flex:1 1 auto;width:auto;min-width:0;margin:0;gap:8px}.topbar>.controls .select{width:150px;min-width:150px;flex:0 0 150px}.topbar>.controls .search{width:min(280px,24vw);min-width:180px;flex:0 1 280px}.topbar>.header-actions{flex:0 0 auto}
    @media(max-width:767px){.topbar>.controls{order:3;width:100%;flex:0 0 auto;flex-wrap:wrap;align-items:stretch}.topbar>.controls .control-left{width:100%;flex-wrap:wrap}.topbar>.controls .control-right{width:100%;flex:1 1 100%;flex-wrap:wrap}.topbar>.controls .select,.topbar>.controls .search{width:100%;min-width:0;flex:1 1 100%}.topbar>.header-actions{order:2}}
    @media(max-width:767px){.header-actions .button:last-child{grid-column:auto}}
    .topbar{display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:14px}.topbar>.brand{justify-self:start}.topbar>.controls{justify-self:stretch;width:100%;justify-content:center}.topbar>.header-actions{justify-self:end}
    @media(min-width:1200px){.topbar>.controls{transform:translateX(clamp(0px,4vw,56px))}}
    .jobs-table.complete{width:max-content;min-width:max(2200px,100%);table-layout:auto}.jobs-table.complete th,.jobs-table.complete td{min-width:120px;max-width:260px;white-space:normal;overflow-wrap:anywhere}.jobs-table.complete th:nth-child(1),.jobs-table.complete td:nth-child(1){min-width:230px;max-width:340px}.jobs-table.complete th:nth-child(2),.jobs-table.complete td:nth-child(2){min-width:145px;max-width:220px}.jobs-table.complete th:nth-child(3),.jobs-table.complete td:nth-child(3){min-width:190px;max-width:280px}.jobs-table.complete th:nth-child(4),.jobs-table.complete td:nth-child(4){min-width:130px;max-width:180px}.jobs-table.complete th:nth-child(6),.jobs-table.complete td:nth-child(6){min-width:150px;max-width:220px}.jobs-table.complete th:nth-child(7),.jobs-table.complete td:nth-child(7){min-width:110px;max-width:160px}.jobs-table.complete th:nth-child(16),.jobs-table.complete td:nth-child(16){min-width:150px;max-width:220px}.jobs-table.complete th:nth-child(18),.jobs-table.complete td:nth-child(18){min-width:150px;max-width:230px}.jobs-table.complete th:nth-child(23),.jobs-table.complete td:nth-child(23){min-width:180px;max-width:240px}.jobs-table.complete .complete-link-cell{white-space:nowrap}.jobs-table.complete .complete-link-cell .link{margin:0 3px 3px 0}.jobs-table.complete .complete-long{min-width:220px;max-width:360px}.jobs-table.complete .complete-long-text{display:-webkit-box;max-height:5.4em;overflow:hidden;line-height:1.35;-webkit-box-orient:vertical;-webkit-line-clamp:4}.jobs-table.complete .column-sort{white-space:normal;line-height:1.15}.jobs-table.complete .complete-contact span{display:block;white-space:nowrap}.jobs-table.complete th:nth-child(3),.jobs-table.complete td:nth-child(3){min-width:210px;max-width:280px}.jobs-table.complete th:nth-child(4),.jobs-table.complete td:nth-child(4){min-width:105px;max-width:125px}.jobs-table.complete th:nth-child(5),.jobs-table.complete td:nth-child(5){min-width:135px;max-width:170px}.jobs-table.complete th:nth-child(6),.jobs-table.complete td:nth-child(6){min-width:95px;max-width:130px}.jobs-table.complete th:nth-child(15),.jobs-table.complete td:nth-child(15){min-width:145px;max-width:210px}.jobs-table.complete th:nth-child(16),.jobs-table.complete td:nth-child(16){min-width:105px;max-width:130px}.jobs-table.complete th:nth-child(17),.jobs-table.complete td:nth-child(17){min-width:145px;max-width:210px}.jobs-table.complete th:nth-child(18),.jobs-table.complete td:nth-child(18){min-width:210px;max-width:330px}.jobs-table.complete th:nth-child(19),.jobs-table.complete td:nth-child(19){min-width:210px;max-width:330px}.jobs-table.complete th:nth-child(20),.jobs-table.complete td:nth-child(20){min-width:130px;max-width:180px}.jobs-table.complete th:nth-child(21),.jobs-table.complete td:nth-child(21){min-width:130px;max-width:170px}.jobs-table.complete th:nth-child(23),.jobs-table.complete td:nth-child(23){min-width:190px;max-width:280px}.jobs-table.complete th:nth-child(24),.jobs-table.complete td:nth-child(24){min-width:105px;max-width:130px}
    .jobs-table thead th{border-bottom:1px solid rgba(196,208,198,.9);box-shadow:0 3px 0 rgba(251,250,246,.96),0 7px 12px -8px rgba(38,61,54,.62)}
    @media(max-width:767px){.topbar{display:flex;align-items:flex-start;flex-direction:column}.topbar>.controls{width:100%;justify-self:auto;justify-content:flex-start;transform:none}.topbar>.header-actions{justify-self:auto}}
    .client-missing{display:inline-flex;align-items:center;gap:5px;color:var(--attention-ink);font-size:12px;font-weight:750;white-space:normal}.client-missing::before{content:"!";display:inline-flex;align-items:center;justify-content:center;width:17px;height:17px;border-radius:50%;background:var(--attention);font-size:11px}.client-diagnostic{margin-top:5px;color:var(--attention-ink);font-size:12px;line-height:1.35}.field-note{margin-top:-1px;color:var(--attention-ink);font-size:12px;line-height:1.35}.field-note.complete{color:var(--muted)}
  </style>
</head>
<body>
  <div id="login" class="login"><div class="login-card"><div class="eyebrow">FloorPlanDrawings · internal</div><h2>Master workspace</h2><p>Sign in with your portal username and password. Chrome can save these credentials for the next visit.</p><form id="loginForm" method="post" action="/api/portal/login" autocomplete="on"><input id="username" name="username" type="text" autocomplete="username" placeholder="Username" required><input id="password" name="password" type="password" autocomplete="current-password" placeholder="Password" required><button id="loginButton" type="submit" class="button">Open workspace</button></form><div id="loginError" class="error" role="alert"></div></div></div>
  <main class="portal">
    <header class="topbar"><div class="brand" aria-label="FloorPlanDrawings Master"><div class="mark"><span>FPD</span><small>MASTER</small></div></div><section id="controls" class="controls"><div class="control-left"><div id="scopeToggle" class="scope-toggle" aria-label="Job scope"><button class="scope-button active" type="button" data-scope="active">Active</button><button class="scope-button" type="button" data-scope="history">History</button><button class="scope-button" type="button" data-scope="all">All</button></div><div class="summary"><strong id="count">0 jobs</strong></div><div id="displayToggle" class="scope-toggle" aria-label="Display mode"><button class="scope-button active" type="button" data-display-mode="essential">Essential</button><button class="scope-button" type="button" data-display-mode="complete">Complete</button></div></div><div class="control-right"><select id="statusFilter" class="select" aria-label="Filter by status"><option value="all">All statuses</option></select><input id="search" class="search" type="search" placeholder="Search client or address" aria-label="Search jobs"></div></section><div class="header-actions"><a class="button secondary nav-active" href="#jobs" aria-current="page">Jobs</a><button id="newIntake" class="button" aria-expanded="false">New intake</button><button id="snapshotToggle" class="button secondary" aria-expanded="false" aria-controls="snapshot">Daily brief</button></div></header>
    <section id="snapshot" class="snapshot" aria-labelledby="snapshotTitle" hidden><div class="snapshot-head"><div><div class="eyebrow">Daily brief</div><h3 id="snapshotTitle">Today’s snapshot</h3></div><span id="snapshotDate" class="snapshot-date"></span></div><div class="snapshot-grid"><div class="snapshot-card"><strong id="snapshotAppointments">—</strong><span>Appointments today</span></div><div class="snapshot-card"><strong id="snapshotDeliveries">—</strong><span>Deliveries due</span></div><div class="snapshot-card"><strong id="snapshotTasks">—</strong><span>Outstanding tasks</span></div><div class="snapshot-card"><strong id="snapshotDecisions">—</strong><span>Decisions needed</span></div></div><div id="snapshotList" class="snapshot-list" aria-live="polite"></div></section>
    <section class="intake-dropbox" id="intakeDropbox" aria-labelledby="intakeTitle" hidden><div class="intake-head"><div><div class="eyebrow">Quick capture</div><h3 id="intakeTitle">Add a request</h3><p>Drop in a text/email file or paste a note. Render will find the address, add Maps/ZIMAS/aerial links, and create or match one Airtable job for review.</p></div></div><div class="intake-form"><div class="intake-field"><label for="manualIntakeText">Request text</label><textarea id="manualIntakeText" placeholder="Example: Hi Anna, can we schedule floorplans at 150 Arlington Dr Pasadena"></textarea></div><div class="intake-field"><label for="manualFile">Drop a request file</label><div id="intakeDropzone" class="intake-dropzone" tabindex="0" role="button"><span><strong>Drag &amp; drop or choose a file</strong>.txt, .eml, or dropped text will be read here.</span></div><input id="manualFile" type="file" accept=".txt,.eml,message/rfc822,text/plain,image/*" hidden><div id="intakeFileNote" class="intake-file-note" aria-live="polite"></div></div><button id="manualIntakeSubmit" type="button" class="button">Add to jobs</button></div><div id="manualIntakeResult" class="intake-result" role="status" aria-live="polite" hidden></div></section>
    <div id="saveConfirmation" class="save-confirm" role="status" aria-live="polite" hidden></div><div id="staleNotice" class="stale" role="status" hidden></div>
    <section id="jobs" class="table-shell" aria-label="FloorPlanDrawings jobs"><table class="jobs-table"><thead><tr id="jobsHead"></tr></thead><tbody id="jobsBody"></tbody></table><div id="jobsCards" class="jobs-cards" aria-live="polite"></div></section>
  </main>
  <div id="backdrop" class="backdrop"><section class="panel" role="dialog" aria-modal="true" aria-labelledby="editTitle"><div class="panel-head"><div><div class="eyebrow">Job editor</div><h2 id="editTitle">Edit job</h2><div id="editAddress" class="mobile-address"></div></div><div class="panel-head-actions"><button id="allInfoButton" type="button" class="button secondary">All info</button><button id="closePanel" class="close" aria-label="Close editor">×</button></div></div><form id="editForm" class="form"><div class="field"><label for="clientName">Client</label><input id="clientName" name="clientName"><div id="clientDiagnostic" class="field-note" role="status"></div></div><div class="field"><label for="status">Status</label><select id="status" name="status"><option>New Request</option><option>Needs Quote</option><option>Quote Sent</option><option>Waiting On Client</option><option>Accepted</option><option>Needs Scheduling</option><option>Scheduled</option><option>Measured</option><option>Drawing In Progress</option><option>Ready For Review</option><option>Delivered</option><option>Invoiced</option><option>Paid</option><option>Lost / Canceled</option><option>In Progress</option><option>Completed</option><option>Calendar Imported</option></select></div><div class="field"><label for="clientEmail">Client email</label><input id="clientEmail" name="clientEmail" type="email"></div><div class="field"><label for="clientPhone">Client phone</label><input id="clientPhone" name="clientPhone" type="tel"></div><div class="field"><label for="assignedMeasurer">Employee assigned</label><input id="assignedMeasurer" name="assignedMeasurer" placeholder="Ricky, Corrie, Sarah"></div><div class="field"><label for="approxSqFt">Estimated square feet</label><input id="approxSqFt" name="approxSqFt" inputmode="numeric"></div><div class="field"><label for="verifiedSqFt">Verified square feet</label><input id="verifiedSqFt" name="verifiedSqFt" inputmode="numeric"></div><div class="field"><label for="appointment">Scheduled visit (Pacific)</label><input id="appointment" name="appointment" type="datetime-local"></div><div class="field"><label for="delivery">Promised delivery</label><input id="delivery" name="delivery" type="date"></div><div class="field full"><label for="accessInfo">Access info</label><textarea id="accessInfo" name="accessInfo"></textarea></div></form><div id="editNotice" class="notice" role="alert" style="display:none"></div><div class="panel-foot"><button id="cancelEdit" type="button" class="button secondary">Cancel</button><button id="saveEdit" type="submit" form="editForm" class="button">Save changes</button></div></section></div><div id="allInfoBackdrop" class="backdrop"><section class="panel all-info-panel" role="dialog" aria-modal="true" aria-labelledby="allInfoTitle"><div class="panel-head"><div><div class="eyebrow">Record detail</div><h2 id="allInfoTitle">All info</h2><div id="allInfoAddress" class="mobile-address"></div></div><button id="closeAllInfo" class="close" aria-label="Close all info">×</button></div><div id="allInfoBody" class="all-info-scroll"></div></section></div><div id="toast" class="toast" role="status" aria-live="polite"></div>
  <script>
    (() => {
      let adminToken = ""; let jobs = []; let status = "all"; let scope = "active"; let displayMode = "essential"; let editing = null; let returnFocus = null; let hasLoaded = false;
      const $ = (id) => document.getElementById(id) || { addEventListener() {} };
      const updateStickyOffset = () => { const controls = $("controls"); if (controls) document.documentElement.style.setProperty("--controls-sticky-offset", Math.ceil(controls.getBoundingClientRect().height) + "px"); const shell = $("jobs"); const table = shell.querySelector("table"); shell.classList.toggle("table-scroll", table.getBoundingClientRect().width > shell.clientWidth + 1); };
      updateStickyOffset(); window.addEventListener("resize", updateStickyOffset); if (window.ResizeObserver) { const observer = new ResizeObserver(updateStickyOffset); observer.observe($("controls")); observer.observe($("jobs")); observer.observe($("jobs").querySelector("table")); }
      const esc = (value) => String(value ?? "").replace(/[&<>\"]/g, (char) => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[char]));
      const fmtDate = (value) => { if (!value) return ""; const date = new Date(value); return Number.isNaN(date.getTime()) ? String(value) : date.toLocaleString("en-US", {timeZone:"America/Los_Angeles",month:"2-digit",day:"2-digit",year:"2-digit",hour:"numeric",minute:"2-digit"}); };
      const fmtDateOnly = (value) => { if (!value) return ""; const text = String(value).slice(0,10); const date = new Date(text + "T12:00:00"); return Number.isNaN(date.getTime()) ? text : date.toLocaleDateString("en-US", {timeZone:"America/Los_Angeles",month:"2-digit",day:"2-digit",year:"2-digit"}); };
      const dateInput = (value) => { if (!value) return ""; const date = new Date(value); if (Number.isNaN(date.getTime())) return ""; const p = (n) => String(n).padStart(2,"0"); const day = new Intl.DateTimeFormat("en-CA", {timeZone:"America/Los_Angeles",year:"numeric",month:"2-digit",day:"2-digit"}).format(date); const parts = new Intl.DateTimeFormat("en-US", {timeZone:"America/Los_Angeles",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).formatToParts(date); const part = (type) => (parts.find((item) => item.type === type) || {}).value || "00"; return day + "T" + p(Number(part("hour"))) + ":" + p(Number(part("minute"))); };
      const showToast = (message) => { $("toast").textContent = message; $("toast").classList.add("show"); setTimeout(() => $("toast").classList.remove("show"), 3200); };
      const request = async (path, options = {}) => { const response = await fetch(path, { credentials:"same-origin", ...options, headers: { "Content-Type":"application/json", "X-Admin-Token":adminToken, ...(options.headers || {}) } }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || ("Request failed (" + response.status + ")")); return body; };
      const statusClass = (value) => /need|review|waiting|lost|cancel/i.test(value || "") ? "warning" : !value ? "muted" : "";
      const displayText = (value) => { const text = String(value ?? "").trim(); return /^(unassigned|not set|not provided|address pending|unavailable)$/i.test(text) ? "" : text; };
      const displayAddress = (job) => { let value = displayText(job.propertyAddress); if (!value) return ""; const zip = value.match(/\\b(?:CA|California)\\s*,?\\s+\\d{5}(?:-\\d{4})?/i); if (zip) value = value.slice(0, zip.index + zip[0].length); else if (displayText(job.zip)) value = value.replace(/[\\s,]+$/, "") + ", " + displayText(job.zip); return value.replace(/[\\s,]+$/, "").trim(); };
      const addressMarkup = (job) => { const value = displayAddress(job); if (!value) return ""; const parts = value.split(",").map((part) => part.trim()).filter(Boolean); let street = parts.shift() || value; let locality = parts.join(", "); const city = displayText(job.city); const state = displayText(job.state); const zip = displayText(job.zip); const canonicalLocality = [city, state, zip].filter(Boolean).join(", "); const embedded = !locality && street.match(/^(.*?\\b(?:street|st|avenue|ave|boulevard|blvd|drive|dr|road|rd|lane|ln|court|ct|place|pl|way|parkway|pkwy|circle|cir|terrace|ter|highway|hwy)\\b)\\s+(.+?)\\s+(CA|California)\\s+(\\d{5}(?:-\\d{4})?)$/i); if (embedded) { street = embedded[1].trim(); locality = [embedded[2], embedded[3], embedded[4]].join(", "); } if (canonicalLocality && (!locality || /^\\d{5}(?:-\\d{4})?$/.test(locality))) locality = canonicalLocality; if (!locality && (city || state || zip)) locality = canonicalLocality; if (city) { const cityIndex = street.toLowerCase().lastIndexOf(city.toLowerCase()); if (cityIndex > 0) street = street.slice(0, cityIndex).replace(/[\\s,]+$/, "").trim(); } return "<span class='address-street'>" + esc(street) + "</span>" + (locality ? "<span class='address-locality'>" + esc(locality) + "</span>" : ""); };
      const normalizedText = (value) => String(value || "").toLowerCase().replace(/[^a-z0-9]+/g, "");
      const addressLikeClient = (name, address) => { const client = normalizedText(name); const property = normalizedText(address); if (!client || !property) return false; if (client === property) return true; const related = client.startsWith(property) || property.startsWith(client); return related && (/(?:ca|california|\d{5})/.test(client) || /(?:street|st|avenue|ave|boulevard|blvd|drive|dr|road|rd|lane|ln|court|ct|place|pl|way|parkway|pkwy|circle|cir|terrace|ter|highway|hwy)/.test(client)); };
      const clientDisplay = (job) => { const name = displayText(job.clientName); if (addressLikeClient(name, job.propertyAddress)) return displayText(contactFallback(job, "email")) || displayText(contactFallback(job, "phone")); return name; };
      const clientCellMarkup = (job) => { const value = clientDisplay(job); if (value) return esc(value); const diagnostic = job.clientDiagnostics && job.clientDiagnostics.reason || "No client name, email, or phone was found."; return "<span class='client-missing' title='" + esc(diagnostic) + "'>Missing client</span>"; };
      const compactScope = (value, service) => { let text = String(value || "").replace(/\\s+(?:appointment preference|client note|timing|access|parking|day-of contact):.*$/i, "").replace(/\\s+/g, " ").trim(); if (!text) return ""; text = text.replace(/^plan:\\s*/i, "").replace(/\\s*3d tour:\\s*(?:yes|no)\\b/ig, "").replace(/^[-|,:\\s]+|[-|,:\\s]+$/g, "").trim(); const serviceText = String(service || "").replace(/\\s+/g, " ").trim(); if (serviceText && text.toLowerCase() === serviceText.toLowerCase()) return ""; if (serviceText && text.toLowerCase().startsWith(serviceText.toLowerCase() + " ")) text = text.slice(serviceText.length).replace(/^[-|,:\\s]+/, "").trim(); return text; };
      const contactMarkup = (job) => { const email = contactFallback(job, "email"); const phone = contactFallback(job, "phone"); return [email, phone].filter(Boolean).map((value) => "<span>" + esc(value) + "</span>").join(""); };
      const sqFtNumber = (value) => { const number = Number(String(value ?? "").replace(/,/g, "")); return Number.isFinite(number) && number > 0 ? number : null; };
      const sqFtMarkup = (job) => { const confirmed = sqFtNumber(job.verifiedSqFt); const estimate = sqFtNumber(job.approxSqFt); const value = confirmed || estimate; return value ? Number(value).toLocaleString() + " sq ft" + (confirmed ? " <span class='confirmed-pill'>Confirmed</span>" : "") : ""; };
      const link = (url, label) => url ? "<a class='link' href='" + esc(url) + "' target='_blank' rel='noopener'>" + label + "</a>" : "<span class='unavailable' aria-hidden='true'></span>";
      const zimasLink = (job) => job.zimasLink || (job.propertyAddress ? "https://zimas.lacity.org/map.asp?address=" + encodeURIComponent(job.propertyAddress) : "");
      const aerialSource = (job) => job.aerialAttachmentUrl || job.mapUrl || job.satellitePhotoLink || "";
      const isGenericMap = (url) => /^https:\\/\\/earth\\.google\\.com\\//i.test(String(url || ""));
      const aerialLink = (job) => !isGenericMap(aerialSource(job)) && aerialSource(job) || (job.propertyAddress ? "https://earth.google.com/web/search/" + encodeURIComponent(job.propertyAddress) : "");
      const aerialLabel = (job) => aerialSource(job) && !isGenericMap(aerialSource(job)) ? "Aerial ↗" : "Map ↗";
      const renderChips = () => {};
      const essentialColumns = [{key:"propertyAddress",label:"Address"},{key:"clientName",label:"Client"},{key:"dateStarted",label:"Date"},{key:"propertyLinks",label:"Property links"},{key:"quoteGiven",label:"Quote given"},{key:"scheduled",label:"Scheduled"},{key:"onCalendar",label:"On calendar"},{key:"confirmationSent",label:"Confirmation"},{key:"reminderSent",label:"Reminder"},{key:"floorplanSent",label:"Floor plan sent"},{key:"paid",label:"Paid"}];
      // Complete is the operator's working view, not a dump of every Airtable
      // field. Raw fields and provenance remain available in the editor.
      const completeColumns = [
        {key:"propertyAddress",label:"Address",format:"address"},{key:"clientName",label:"Client"},{key:"contact",label:"Contact",format:"contact"},{key:"dateStarted",label:"Requested",format:"dateOnly"},
        {key:"status",label:"Status",format:"status"},{key:"quoteAmount",label:"Quote",format:"quote"},{key:"quoteWorkflow",label:"Quote given",format:"workflow",workflowKey:"quoteGiven"},{key:"scheduled",label:"Scheduled",format:"workflow"},{key:"onCalendar",label:"On calendar",format:"workflow"},{key:"confirmationSent",label:"Confirmation sent",format:"workflow"},{key:"reminderSent",label:"Reminder sent",format:"workflow"},{key:"floorplanSent",label:"Floor plan sent",format:"workflow"},{key:"paid",label:"Paid",format:"workflow"},{key:"paymentStatus",label:"Payment status"},
        {key:"visit",label:"Visit",format:"visit"},{key:"deliveryDate",label:"Delivery",format:"dateOnly"},{key:"service",label:"Service"},{key:"scope",label:"Scope",format:"long"},{key:"accessInfo",label:"Access",format:"long"},{key:"assignedMeasurer",label:"Employee"},{key:"squareFeet",label:"Square feet",format:"sqft"},
        {key:"propertyLinks",label:"Property links",format:"links"},{key:"clientResponse",label:"Client response",format:"long"},{key:"followUpDate",label:"Follow-up",format:"dateOnly"}
      ];
      const workflowState = (job) => { const statusText = String(job.status || ""); const details = job.detailFields || {}; const has = (value) => Boolean(String(value || "").trim()); const quoteGiven = has(job.quoteGiven) || has(job.quoteSentDate) || /quote sent|quote ready|needs scheduling|scheduled|accepted|calendar imported|measured|delivered|completed|\bpaid\b/i.test(statusText); const scheduled = has(job.appointmentDateTime) || has(job.appointmentStart) || /scheduled|calendar imported|measured|delivered|completed|\bpaid\b/i.test(statusText); const onCalendar = has(job.calendarEventId) || /calendar/i.test(String(job.sourceChannels || "")) || /calendar imported/i.test(statusText); const confirmationSent = has(job.confirmationSentAt) || has(details["Client Confirmation Sent At"]) || has(details["Confirmation Sent At"]); const reminderSent = has(job.reminderSentAt) || has(details["Client Reminder Sent At"]) || has(details["Reminder Sent At"]); const floorplanSent = has(job.finalFiles) || has(details["Final Files"]) || has(details["Files / Final Deliverables"]) || /delivered|completed|\bpaid\b/i.test(statusText); const paid = /^(paid|complete|completed)$/i.test(String(job.paymentStatus || "").trim()) || /\bpaid\b/i.test(statusText); return {quoteGiven,scheduled,onCalendar,confirmationSent,reminderSent,floorplanSent,paid}; };
      const workflowCheck = (value, label) => "<span class='workflow-check " + (value ? "done" : "") + "' title='" + esc(label + (value ? ": yes" : ": not recorded")) + "' aria-label='" + esc(label + (value ? ": yes" : ": not recorded")) + "'>" + (value ? "✓" : "—") + "</span>";
      const currentColumns = () => displayMode === "essential" ? essentialColumns : completeColumns;
      const renderTableHead = () => { const columns = currentColumns(); $("jobsHead").innerHTML = columns.map((column) => "<th scope='col'><button class='column-sort' type='button' data-sort-key='" + esc(column.key) + "'>" + esc(column.label) + " <span class='column-sort-indicator' aria-hidden='true'></span></button></th>").join(""); document.querySelector(".jobs-table").classList.toggle("essential", displayMode === "essential"); document.querySelector(".jobs-table").classList.toggle("complete", displayMode === "complete"); };
      const renderStatusOptions = () => { const values = Array.from(new Set(jobs.map((job) => job.status).filter(Boolean))).sort(); $("statusFilter").innerHTML = "<option value='all'>All statuses</option>" + values.map((value) => "<option value='" + esc(value) + "'>" + esc(value) + "</option>").join(""); $("statusFilter").value = values.includes(status) ? status : "all"; status = $("statusFilter").value; };
      const terminalStatus = (value) => /^(delivered|completed|paid|lost\\s*\\/\\s*canceled)$/i.test(String(value || "").trim());
      const renderScope = () => document.querySelectorAll("[data-scope]").forEach((button) => { const active = button.dataset.scope === scope; button.classList.toggle("active", active); button.setAttribute("aria-pressed", String(active)); });
      let sortKey = "dateStarted"; let sortDirection = "desc";
      const sortValue = (job, key) => { if (key === "propertyLinks") return [job.googleMapsLink, zimasLink(job), aerialLink(job)].filter(Boolean).length; if (key === "squareFeet") return sqFtNumber(job.verifiedSqFt) || sqFtNumber(job.approxSqFt) || 0; if (key === "quoteAmount") return Number(String(job.finalQuote || job.clientFacingQuote || job.suggestedQuote || "").replace(/[^0-9.-]/g, "")) || 0; if (key === "visit") key = "appointmentDateTime"; if (key === "quoteWorkflow") key = "quoteGiven"; if (["quoteGiven","scheduled","onCalendar","confirmationSent","reminderSent","floorplanSent","paid"].includes(key)) return workflowState(job)[key] ? 1 : 0; const value = key.startsWith("detail:") ? (job.detailFields || {})[key.slice(7)] : (key === "appointmentDateTime" ? (job.appointmentDateTime || job.appointmentStart || job.nextAvailable) : job[key]); if (key === "dateStarted" || key === "appointmentDateTime" || key === "appointmentStart" || key === "nextAvailable" || key === "deliveryDate" || key === "followUpDate" || key === "quoteSentDate" || key === "confirmationSentAt" || key === "reminderSentAt" || key === "calendarEventStart") { const timestamp = value ? new Date(value).getTime() : 0; return Number.isFinite(timestamp) ? timestamp : 0; } return String(value ?? "").trim().toLowerCase(); };
      const filteredJobs = () => { const term = $("search").value.trim().toLowerCase(); return jobs.filter((job) => { const statusMatch = status === "all" || job.status === status; const scopeMatch = scope === "all" || (scope === "history" ? terminalStatus(job.status) : !terminalStatus(job.status)); const searchable = [job.clientName,job.propertyAddress,job.city,job.zip,job.status,job.assignedMeasurer,job.quoteZone,...Object.values(job.detailFields || {})]; const textMatch = !term || searchable.some((value) => String(value || "").toLowerCase().includes(term)); return scopeMatch && statusMatch && textMatch; }).sort((left, right) => { const a = sortValue(left, sortKey); const b = sortValue(right, sortKey); const aEmpty = a === "" || a === 0; const bEmpty = b === "" || b === 0; if (aEmpty !== bEmpty) return aEmpty ? 1 : -1; const comparison = typeof a === "number" && typeof b === "number" ? a - b : String(a).localeCompare(String(b), undefined, { numeric: true, sensitivity: "base" }); return sortDirection === "asc" ? comparison : -comparison; }); };
      const renderSortIndicators = () => document.querySelectorAll("[data-sort-key]").forEach((button) => { const active = button.dataset.sortKey === sortKey; button.classList.toggle("sort-active", active); const indicator = button.querySelector(".column-sort-indicator"); if (indicator) indicator.textContent = active ? (sortDirection === "asc" ? "↑" : "↓") : ""; button.setAttribute("aria-label", button.textContent.trim() + (active ? (sortDirection === "asc" ? ", ascending" : ", descending") : ", sort")); });
      const completeCellValue = (job, column) => {
        if (column.key === "visit") return job.appointmentDateTime || job.appointmentStart || job.nextAvailable;
        if (column.key === "squareFeet") return sqFtNumber(job.verifiedSqFt) || sqFtNumber(job.approxSqFt);
        if (column.key === "quoteAmount") return job.finalQuote || job.clientFacingQuote || job.suggestedQuote;
        if (column.key === "clientName") return clientDisplay(job);
        if (column.key === "scope") return compactScope(job.scope, job.service);
        if (column.key === "clientNotes") return job.clientNotes || "";
        return column.detailKey ? (job.detailFields || {})[column.detailKey] : job[column.key];
      };
      const completeText = (value) => Array.isArray(value) ? value.join(", ") : value && typeof value === "object" ? JSON.stringify(value) : String(value ?? "");
      const completeCellMarkup = (job, column) => { const state = workflowState(job); const value = completeCellValue(job, column); if (column.key === "clientName") return clientCellMarkup(job); if (column.format === "address") return addressMarkup(job); if (column.format === "contact") return contactMarkup(job); if (column.format === "links") return "<div>" + link(job.googleMapsLink,"Maps") + link(zimasLink(job),"ZIMAS") + link(aerialLink(job),aerialLabel(job).replace(" image", "").replace(" ↗", "")) + "</div>"; if (column.format === "workflow") return workflowCheck(state[column.workflowKey || column.key]," " + column.label); if (column.format === "status") return displayText(value) ? "<span class='status " + statusClass(value) + "'>" + esc(displayText(value)) + "</span>" : ""; if (column.format === "date") return esc(fmtDate(value)); if (column.format === "dateOnly") return esc(fmtDateOnly(value)); if (column.format === "visit") return value && /^\d{4}-\d{2}-\d{2}/.test(String(value)) ? esc(fmtDate(value)) : esc(completeText(value)); if (column.format === "sqft") return value ? Number(value).toLocaleString() + " sq ft" + (sqFtNumber(job.verifiedSqFt) ? " <span class='confirmed-pill'>Confirmed</span>" : "") : ""; if (column.format === "quote") return value ? esc(completeText(value)) : ""; if (column.format === "long") return value ? "<span class='complete-long-text'>" + esc(completeText(value)) + "</span>" : ""; if (column.format === "url") return value ? link(value,"Open ↗") : ""; return esc(completeText(value)); };
      const rowMarkup = (job) => { const columns = currentColumns(); if (displayMode === "essential") { const state = workflowState(job); return "<tr data-edit-row='" + esc(job.recordId) + "' title='Double-click to edit'><td class='address'>" + addressMarkup(job) + "</td><td class='client'>" + clientCellMarkup(job) + (job.clientDiagnostics && job.clientDiagnostics.status !== "complete" ? "<div class='client-diagnostic' title='" + esc(job.clientDiagnostics.reason || "") + "'>" + esc(job.clientDiagnostics.status === "missing" ? "Needs client details" : "Contact details incomplete") + "</div>" : "") + "</td><td class='date'>" + esc(fmtDateOnly(job.dateStarted)) + "</td><td class='property-links'><div>" + link(job.googleMapsLink,"Maps") + link(zimasLink(job),"ZIMAS") + link(aerialLink(job),aerialLabel(job).replace(" image", "").replace(" ↗", "")) + "</div></td><td>" + workflowCheck(state.quoteGiven,"Quote given") + "</td><td>" + workflowCheck(state.scheduled,"Scheduled") + "</td><td>" + workflowCheck(state.onCalendar,"On calendar") + "</td><td>" + workflowCheck(state.confirmationSent,"Confirmation sent") + "</td><td>" + workflowCheck(state.reminderSent,"Reminder sent") + "</td><td>" + workflowCheck(state.floorplanSent,"Floor plan sent") + "</td><td>" + workflowCheck(state.paid,"Paid") + "</td></tr>"; } return "<tr data-edit-row='" + esc(job.recordId) + "' title='Double-click to edit'>" + columns.map((column) => { const classes = [column.format === "address" ? "address" : "", column.format === "contact" ? "complete-contact" : "", column.format === "url" || column.format === "links" ? "complete-link-cell" : "", column.format === "long" ? "complete-long" : ""]; return "<td class='" + classes.filter(Boolean).join(" ") + "'>" + completeCellMarkup(job, column) + "</td>"; }).join("") + "</tr>"; };
      const cardMarkup = (job) => "<article class='mobile-card' data-edit-row='" + esc(job.recordId) + "' title='Double-click to edit'><div class='mobile-card-head'><div><div class='eyebrow'>" + esc(job.service || job.workflow || "Floor plan request") + "</div><h3>" + clientCellMarkup(job) + "</h3><div class='mobile-address'>" + addressMarkup(job) + "</div></div>" + (displayText(job.status) ? "<span class='status " + statusClass(job.status) + "'>" + esc(displayText(job.status)) + "</span>" : "") + "</div><div class='mobile-meta'><div><span>Date</span><strong>" + esc(fmtDateOnly(job.dateStarted)) + "</strong></div><div><span>Employee</span><strong>" + esc(displayText(job.assignedMeasurer)) + "</strong></div><div><span>Visit</span><strong>" + esc(fmtDate(job.appointmentDateTime || job.appointmentStart)) + "</strong></div><div><span>Delivery</span><strong>" + esc(fmtDateOnly(job.deliveryDate)) + "</strong></div></div><div class='mobile-area'><span class='eyebrow'>Square footage</span><strong>" + sqFtMarkup(job) + "</strong></div><div class='mobile-links'>" + link(job.googleMapsLink,"Maps") + link(zimasLink(job),"ZIMAS") + link(aerialLink(job),aerialLabel(job).replace(" ↗", "")) + "</div><details><summary>Access and record details</summary><p>" + esc(displayText(job.accessInfo)) + "</p></details></article>";
      const bindEditors = () => document.querySelectorAll("[data-edit-row]").forEach((row) => row.addEventListener("dblclick", (event) => { if (event.target.closest("a,button,input,select,textarea")) return; openEditor(jobs.find((job) => job.recordId === row.dataset.editRow), row); }));
      const renderJobs = () => { const visible = filteredJobs(); renderTableHead(); $("count").textContent = visible.length + (visible.length === 1 ? " job" : " jobs"); const emptyHtml = "<div class='empty'><strong>No jobs match this view.</strong><br>Try another status or search term.<br><button class='clear' data-clear-empty type='button'>Clear filters</button></div>"; $("jobsBody").innerHTML = visible.length ? visible.map(rowMarkup).join("") : "<tr><td colspan='" + currentColumns().length + "'>" + emptyHtml + "</td></tr>"; $("jobsCards").innerHTML = visible.length ? visible.map(cardMarkup).join("") : emptyHtml; renderSortIndicators(); document.querySelectorAll("[data-clear-empty]").forEach((button) => button.addEventListener("click", clearFilters)); bindEditors(); updateStickyOffset(); };
      const localDateKey = (value) => { if (!value) return ""; const date = new Date(value); if (Number.isNaN(date.getTime())) return String(value).slice(0,10); return new Intl.DateTimeFormat("en-CA", {timeZone:"America/Los_Angeles",year:"numeric",month:"2-digit",day:"2-digit"}).format(date); };
      const renderSnapshot = () => { const today = localDateKey(new Date()); const appointments = jobs.filter((job) => localDateKey(job.appointmentDateTime || job.appointmentStart) === today); const deliveries = jobs.filter((job) => localDateKey(job.deliveryDate) === today); const outstanding = jobs.filter((job) => !terminalStatus(job.status)); const decisions = jobs.filter((job) => /new request|needs quote|needs scheduling|waiting|ready for review|approval/i.test(job.status || "")); $("snapshotAppointments").textContent = appointments.length; $("snapshotDeliveries").textContent = deliveries.length; $("snapshotTasks").textContent = outstanding.length; $("snapshotDecisions").textContent = decisions.length; $("snapshotDate").textContent = new Intl.DateTimeFormat("en-US", {timeZone:"America/Los_Angeles",month:"2-digit",day:"2-digit",year:"2-digit"}).format(new Date()); const items = [...appointments.slice(0,3).map((job) => ({label:"Visit", job, attention:false})), ...deliveries.slice(0,2).map((job) => ({label:"Due", job, attention:true})), ...decisions.slice(0,2).map((job) => ({label:"Decision", job, attention:true}))]; $("snapshotList").innerHTML = items.length ? items.map(({label,job,attention}) => "<span class='snapshot-item " + (attention ? "attention" : "") + "'><b>" + esc(label) + "</b> " + esc(job.clientName || job.propertyAddress || "Unassigned job") + "</span>").join("") : "<span class='snapshot-item'><b>Clear</b> Nothing needs attention today</span>"; };
      const renderLoading = () => { $("jobsBody").innerHTML = Array.from({length:5}, () => "<tr><td colspan='" + currentColumns().length + "'><div class='skeleton' aria-label='Loading jobs'></div></td></tr>").join(""); $("jobsCards").innerHTML = Array.from({length:2}, () => "<div class='mobile-card'><div class='skeleton' aria-label='Loading job'></div></div>").join(""); };
      const clearFilters = () => { status = "all"; $("search").value = ""; renderStatusOptions(); renderScope(); renderJobs(); };
      const loadJobs = async () => { renderLoading(); try { const body = await request("/api/portal/jobs?limit=250"); jobs = body.jobs || []; hasLoaded = true; renderStatusOptions(); renderSnapshot(); renderJobs(); $("staleNotice").hidden = true; $("login").classList.add("hidden"); } catch (error) { if (hasLoaded && jobs.length) { $("staleNotice").textContent = "Showing the last successful refresh. " + error.message + " Try Refresh jobs again."; $("staleNotice").hidden = false; renderSnapshot(); renderJobs(); showToast("Refresh failed — showing saved view"); } else { $("loginError").textContent = error.message; $("login").classList.remove("hidden"); $("jobsBody").innerHTML = ""; $("jobsCards").innerHTML = "<div class='empty'><strong>Jobs could not be loaded.</strong><br>Sign in again and try again.</div>"; } } };
      const allInfoEntries = (job) => { const fields = { ...(job && job.detailFields || {}) }; const fallback = { "Record ID": job.recordId, "Property Address": job.propertyAddress, City: job.city, State: job.state, Zip: job.zip, "Client Name": job.clientName, "Client Email": job.clientEmail, "Client Phone": job.clientPhone, "Client data status": job.clientDiagnostics && job.clientDiagnostics.status, "Client data diagnosis": job.clientDiagnostics && job.clientDiagnostics.reason, "Client data source": job.clientDiagnostics && job.clientDiagnostics.source, "Contact Emails": (job.contactEmails || []).join(", "), "Contact Phones": (job.contactPhones || []).join(", "), Status: job.status, "Source Channels": job.sourceChannels, "Google Maps Link": job.googleMapsLink, "ZIMAS Link": job.zimasLink, "Aerial Link": aerialLink(job), "Request Started": job.dateStarted, "Appointment": job.appointmentDateTime || job.appointmentStart, "Delivery Date": job.deliveryDate, "Assigned Measurer": job.assignedMeasurer, "Access Info": job.accessInfo, "Original Request": job.originalRequest }; Object.entries(fallback).forEach(([key, value]) => { if (value !== undefined && value !== null && String(value).trim() && !fields[key]) fields[key] = String(value); }); return Object.entries(fields).filter(([, value]) => String(value || "").trim()); };
      const openAllInfo = (job) => { if (!job) return; $("allInfoTitle").textContent = displayText(job.clientName) || "All info"; $("allInfoAddress").textContent = displayAddress(job); const rows = allInfoEntries(job).map(([key, value]) => "<tr><th scope='row'>" + esc(key) + "</th><td>" + esc(value) + "</td></tr>").join(""); $("allInfoBody").innerHTML = "<table class='all-info-table'><tbody>" + (rows || "<tr><td>No record details available.</td></tr>") + "</tbody></table>"; $("allInfoBackdrop").classList.add("open"); };
      const closeAllInfo = () => $("allInfoBackdrop").classList.remove("open");
      const contactFallback = (job, kind) => { const key = kind === "email" ? "Client Email" : "Client Phone"; const values = [job && job[kind === "email" ? "clientEmail" : "clientPhone"], job && job.detailFields && job.detailFields[key], ...((job && Array.isArray(job[kind === "email" ? "contactEmails" : "contactPhones"]) && job[kind === "email" ? "contactEmails" : "contactPhones"]) || [])].map((value) => String(value || "").trim()).filter(Boolean); const direct = values.find((value) => kind === "email" ? /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(value) && !/annaellison|floorplandrawings|noreply|no-reply/i.test(value) : /\\d{3}[\\s.-]?\\d{3}[\\s.-]?\\d{4}/.test(value) && value.replace(/\\D/g, "") !== "4436213024"); if (direct) return direct; const raw = JSON.stringify(job || {}); if (kind === "email") { const match = raw.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}/ig); return (match || []).find((value) => !/annaellison|floorplandrawings|noreply|no-reply/i.test(value)) || ""; } const match = raw.match(/(?:\\+?1[\\s.-]?)?(?:\\(?\\d{3}\\)?[\\s.-]?)\\d{3}[\\s.-]?\\d{4}/g); return (match || []).find((value) => value.replace(/\\D/g, "") !== "4436213024") || ""; };
      const openEditor = (job, source) => { if (!job) return; editing = job; returnFocus = source || document.activeElement; const clientEmail = contactFallback(job, "email"); const clientPhone = contactFallback(job, "phone"); $("editTitle").textContent = displayText(job.clientName) || "Edit job"; $("editAddress").textContent = displayAddress(job); $("clientName").value = job.clientName || ""; $("clientEmail").value = clientEmail; $("clientPhone").value = clientPhone; $("status").value = job.status || "New Request"; $("assignedMeasurer").value = job.assignedMeasurer || ""; $("approxSqFt").value = job.approxSqFt || ""; $("verifiedSqFt").value = job.verifiedSqFt || ""; $("appointment").value = dateInput(job.appointmentDateTime || job.appointmentStart); $("delivery").value = job.deliveryDate ? String(job.deliveryDate).slice(0,10) : ""; $("accessInfo").value = job.accessInfo || ""; const diagnostic = job.clientDiagnostics; $("clientDiagnostic").textContent = diagnostic ? diagnostic.reason : ""; $("clientDiagnostic").classList.toggle("complete", !diagnostic || diagnostic.status === "complete"); $("editNotice").style.display = "none"; $("backdrop").classList.add("open"); setTimeout(() => $("clientName").focus(), 0); };
      const closeEditor = () => { editing = null; $("backdrop").classList.remove("open"); if (returnFocus && typeof returnFocus.focus === "function") returnFocus.focus(); };
      document.querySelectorAll("[data-scope]").forEach((button) => button.addEventListener("click", () => { scope = button.dataset.scope; renderScope(); renderJobs(); }));
      document.querySelectorAll("[data-display-mode]").forEach((button) => button.addEventListener("click", () => { displayMode = button.dataset.displayMode; document.querySelectorAll("[data-display-mode]").forEach((item) => item.classList.toggle("active", item.dataset.displayMode === displayMode)); renderJobs(); }));
      $("jobsHead").addEventListener("click", (event) => { const button = event.target.closest("[data-sort-key]"); if (!button) return; const nextKey = button.dataset.sortKey; if (nextKey === sortKey) sortDirection = sortDirection === "asc" ? "desc" : "asc"; else { sortKey = nextKey; sortDirection = "asc"; } renderJobs(); });
      $("loginForm").addEventListener("submit", async (event) => { event.preventDefault(); const button = $("loginButton"); button.disabled = true; $("loginError").textContent = ""; try { const response = await fetch("/api/portal/login", { credentials:"same-origin", method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ username:$("username").value.trim(), password:$("password").value }) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || ("Login failed (" + response.status + ")")); $("password").value = ""; await loadJobs(); } catch (error) { $("loginError").textContent = error.message || "Login failed"; } finally { button.disabled = false; } }); $("refresh").addEventListener("click", loadJobs); $("logout").addEventListener("click", () => { fetch("/api/portal/logout", { credentials:"same-origin", method:"POST" }).catch(() => {}); adminToken = ""; jobs = []; hasLoaded = false; $("username").value = ""; $("password").value = ""; $("loginError").textContent = ""; $("login").classList.remove("hidden"); }); $("search").addEventListener("input", renderJobs); $("statusFilter").addEventListener("change", (event) => { status = event.target.value; renderJobs(); }); $("clearFilters").addEventListener("click", clearFilters); $("closePanel").addEventListener("click", closeEditor); $("cancelEdit").addEventListener("click", closeEditor); $("allInfoButton").addEventListener("click", () => openAllInfo(editing)); $("closeAllInfo").addEventListener("click", closeAllInfo); $("backdrop").addEventListener("click", (event) => { if (event.target === $("backdrop")) closeEditor(); }); $("allInfoBackdrop").addEventListener("click", (event) => { if (event.target === $("allInfoBackdrop")) closeAllInfo(); }); document.addEventListener("keydown", (event) => { if (event.key === "Escape" && $("allInfoBackdrop").classList.contains("open")) closeAllInfo(); else if (event.key === "Escape" && editing) closeEditor(); }); $("editForm").addEventListener("submit", async (event) => { event.preventDefault(); if (!editing) return; const button = $("saveEdit"); const notice = $("editNotice"); button.disabled = true; notice.style.display = "none"; try { const fields = { "Client Name": $("clientName").value.trim(), "Client Email": $("clientEmail").value.trim(), "Client Phone": $("clientPhone").value.trim(), Status: $("status").value, "Assigned Measurer": $("assignedMeasurer").value.trim(), "Approx Sq Ft": $("approxSqFt").value ? Number($("approxSqFt").value) : null, "Verified Sq Ft": $("verifiedSqFt").value ? Number($("verifiedSqFt").value) : null, "Appointment Date/Time": $("appointment").value ? new Date($("appointment").value).toISOString() : null, "Delivery Date": $("delivery").value || null, "Access Info": $("accessInfo").value.trim() }; const client = $("clientName").value.trim() || "job"; const verified = $("verifiedSqFt").value ? Number($("verifiedSqFt").value).toLocaleString() + " sq ft verified" : "square footage still unverified"; await request("/api/portal/jobs/" + encodeURIComponent(editing.recordId), { method:"PATCH", body:JSON.stringify({ fields }) }); closeEditor(); await loadJobs(); $("saveConfirmation").textContent = "Changes saved for " + client + " — " + verified + "."; $("saveConfirmation").hidden = false; showToast("Changes saved"); } catch (error) { notice.textContent = error.message + " Your draft is still here; retry when ready."; notice.style.display = "block"; } finally { button.disabled = false; } }); const restoreSession = async () => { try { const response = await fetch("/api/portal/session", { credentials:"same-origin" }); const body = await response.json().catch(() => ({})); if (body.authenticated) await loadJobs(); } catch {} }; restoreSession();
    })();
  </script>
  <script>
    (() => { const toggle = document.getElementById("snapshotToggle"); const snapshot = document.getElementById("snapshot"); if (!toggle || !snapshot) return; toggle.addEventListener("click", () => { const opening = snapshot.hidden; snapshot.hidden = !opening; toggle.textContent = opening ? "Hide daily brief" : "Daily brief"; toggle.setAttribute("aria-expanded", String(opening)); if (opening) snapshot.scrollIntoView({ behavior: "smooth", block: "start" }); }); })();
  </script>
  <script>
    (() => {
      const text = document.getElementById("manualIntakeText");
      const submit = document.getElementById("manualIntakeSubmit");
      const result = document.getElementById("manualIntakeResult");
      const dropzone = document.getElementById("intakeDropzone");
      const file = document.getElementById("manualFile");
      const fileNote = document.getElementById("intakeFileNote");
      const jump = document.getElementById("newIntake");
      if (!text || !submit || !result || !dropzone || !file) return;
      const show = (message, error = false) => { result.textContent = message; result.hidden = false; result.classList.toggle("error", error); };
      const loadOcr = () => new Promise((resolve, reject) => { if (window.Tesseract) return resolve(window.Tesseract); const script = document.createElement("script"); script.src = "https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js"; script.onload = () => resolve(window.Tesseract); script.onerror = () => reject(new Error("OCR could not be loaded")); document.head.appendChild(script); });
      const readFile = async (picked) => { if (!picked) return; if (picked.type.startsWith("image/")) { fileNote.textContent = picked.name + " received. Reading screenshot…"; try { const tesseract = await loadOcr(); const result = await tesseract.recognize(picked, "eng", { logger: (message) => { if (message && message.status === "recognizing text") fileNote.textContent = picked.name + " — OCR " + Math.round((message.progress || 0) * 100) + "%"; } }); const recognized = String(result && result.data && result.data.text || "").trim(); if (!recognized) throw new Error("No text was detected"); text.value = recognized.slice(0, 20000); fileNote.textContent = picked.name + " — OCR complete. Review the text, then add to jobs."; text.focus(); } catch (error) { fileNote.textContent = "OCR failed. Paste the message text below instead."; show(error.message || "OCR failed", true); } return; } const reader = new FileReader(); reader.onload = () => { text.value = String(reader.result || "").slice(0, 20000); fileNote.textContent = picked.name + " loaded into request text."; text.focus(); }; reader.onerror = () => show("That file could not be read.", true); reader.readAsText(picked); };
      const intake = document.getElementById("intakeDropbox");
      jump && jump.addEventListener("click", () => { const opening = intake.hidden; intake.hidden = !opening; jump.textContent = opening ? "Close intake" : "New intake"; jump.setAttribute("aria-expanded", String(opening)); if (opening) { intake.scrollIntoView({ behavior: "smooth", block: "start" }); setTimeout(() => text.focus(), 250); } });
      dropzone.addEventListener("click", () => file.click()); dropzone.addEventListener("keydown", (event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); file.click(); } }); file.addEventListener("change", () => readFile(file.files && file.files[0]));
      ["dragenter", "dragover"].forEach((eventName) => dropzone.addEventListener(eventName, (event) => { event.preventDefault(); dropzone.classList.add("dragging"); })); ["dragleave", "drop"].forEach((eventName) => dropzone.addEventListener(eventName, (event) => { event.preventDefault(); dropzone.classList.remove("dragging"); })); dropzone.addEventListener("drop", (event) => { const droppedText = event.dataTransfer && event.dataTransfer.getData("text/plain"); if (droppedText) { text.value = droppedText.slice(0, 20000); fileNote.textContent = "Dropped text loaded into request text."; text.focus(); return; } readFile(event.dataTransfer && event.dataTransfer.files && event.dataTransfer.files[0]); });
      submit.addEventListener("click", async () => { submit.disabled = true; show("Reading request…"); try { const response = await fetch("/api/portal/intake", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ text: text.value }) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Request could not be saved"); show(body.message || (body.action === "matched" ? "Matched an existing job." : "Added a new job.")); text.value = ""; fileNote.textContent = ""; setTimeout(() => window.location.reload(), 700); } catch (error) { show(error.message, true); } finally { submit.disabled = false; } });
    })();
  </script>
</body>
</html>`;
}

module.exports = { portalPage };
